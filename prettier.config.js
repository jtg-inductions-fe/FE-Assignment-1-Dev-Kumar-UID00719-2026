const config = {
    trailingComma: 'all',
    tabWidth: 4,
    semi: true,
    singleQuote: true,
    endOfLine: 'lf',
};

export default config;

// robots.txt
// different types of js files like config.js, mjs etc
// are there packages for resizeTo.js??
// normalize.css
// dependencies and devdependencies
// npmrc and nvmrc what else can be added there??
